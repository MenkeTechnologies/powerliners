# Allowlist of Rust fn names under src/ported/ that are NOT in
# docs/powerline_py_functions.txt. Each entry must be approved by the
# maintainer before being added here. The commit that adds the entry
# must reference the approval ("approved 2026-MM-DD").
#
# Adding a name here without prior maintainer approval is itself a
# violation of docs/PORT.md Rule 0.
#
# Format: one fn name per line. Comments start with #.

# Approved 2026-05-28: accessor fns for module-level path constants from
# powerline/config.py:7-10. The Python source declares POWERLINE_ROOT /
# BINDINGS_DIRECTORY / TMUX_CONFIG_DIRECTORY / DEFAULT_SYSTEM_CONFIG_DIR
# as module-level bindings evaluated at import time. Rust has no
# equivalent module-load hook, so the values are exposed as lazy-init
# accessor fns wrapping `OnceLock`. Names match the Python identifiers
# byte-for-byte (uppercase preserved per PORT.md Rule A).
POWERLINE_ROOT
BINDINGS_DIRECTORY
TMUX_CONFIG_DIRECTORY
DEFAULT_SYSTEM_CONFIG_DIR

# Approved 2026-05-28: constructor for the REMOVE_THIS_KEY sentinel from
# powerline/lib/dict.py:5. Python declares it as `REMOVE_THIS_KEY = object()`
# — an opaque sentinel used by mergedicts to signal key deletion. Rust
# does not have Python's `object()` sentinel idiom, so we expose
# REMOVE_THIS_KEY as a fn that returns a well-known JSON marker value.
# Name matches the Python identifier byte-for-byte.
REMOVE_THIS_KEY

# Approved 2026-05-28: per-type variants of safe_unicode() from
# powerline/lib/unicode.py:121. Python's safe_unicode(s) accepts bytes,
# str, or arbitrary object and dispatches at runtime via isinstance +
# duck-typed __str__/__repr__. Rust cannot type-dispatch on parameter
# type without an extra trait/macro, so the cascade is split into
# safe_unicode_str (for &str), safe_unicode_bytes (for &[u8]), and
# safe_unicode<T: Display> (catch-all). Together they cover the same
# inputs Python's single safe_unicode handles.
safe_unicode_str
safe_unicode_bytes

# Approved 2026-05-28: accessor fns for module-level pre-compiled regex
# constants from powerline/bindings/tmux/__init__.py:66-68. Python
# evaluates re.compile(...) at module import; Rust uses OnceLock-backed
# accessor fns. Names match the Python identifiers byte-for-byte
# (uppercase preserved per PORT.md Rule A).
NON_DIGITS
DIGITS
NON_LETTERS

# Approved 2026-05-28: py_round helper exposing Python 3's banker's
# rounding semantics (`round(2.5) == 2` half-to-even) in Rust. Required
# for byte-identical parity at every call site that ports Python's
# `int(round(x))` (colorscheme.pick_gradient_value:32, and any future
# port that does the same). Rust's `f64::round` rounds half-away-from-
# zero, which gives observable divergence (caught by
# parity_pick_gradient_value at gradient_level=25). Internal helper, not
# a name-port. Approval recorded after the parity test caught the bug.
py_round

# Approved 2026-05-28: accessor fn for module-level dict from
# powerline/theme.py:40. Python declares `expand_functions = {'l': ...,
# 'r': ..., 'c': ...}` as a module-level dict mapping align-char to
# expand fn. Rust exposes it as a match-fn over the same key set so
# callers can do `expand_functions('l')` instead of indexing a static
# HashMap. Name matches the Python identifier byte-for-byte.
expand_functions

# Approved 2026-05-28: per-shape variants of parsedotval() from
# powerline/lib/overrides.py:47. Python's parsedotval accepts EITHER a
# str (parsed via keyvaluesplit) OR a pre-split (o, val_str) tuple
# (re-parsed via parse_value); dispatch via `if type(s) is tuple`. Rust
# can't isinstance-dispatch on a sum type without a runtime tag, so
# the cascade is split into parsedotval_str (str input) and
# parsedotval_tuple (pre-split). build_nested is the internal helper
# that both call to assemble the {keys[0]: {...: val}} shape (matches
# the inline body at py:58-68 — Python keeps it inlined twice, Rust
# factors it once since both callers want the same shape).
parsedotval_str
parsedotval_tuple
build_nested

# Approved 2026-05-28: replacement entry point for the closure that
# Python's `memoize.__call__()` returns (`powerline/lib/memoize.py:21`).
# Python's `__call__` returns a wrapped function; the caller treats the
# memoize instance as a decorator. Rust can't return a closure that
# borrows from self for the lifetime of the cache without complex
# lifetime gymnastics, so the call-site shape is collapsed to a plain
# method: callers pass kwargs + a compute closure, the method handles
# the cache lookup/store internally. Same observable semantics as the
# Python decorator; only the call syntax differs.
get_or_compute

# Approved 2026-05-28: module-level binding from powerline/lib/url.py:7
# (Python: `from urllib.parse import urlencode as urllib_urlencode`).
# Re-exported via Python's `as` alias mechanism — the underlying fn is
# `urllib.parse.urlencode`. Rust ports it as a fn under the alias name
# (matching the upstream module namespace). The Rust impl is a small
# inline percent-encoder; the `percent_encode` byte-walker is the
# internal helper (Python uses `urllib.parse.quote_plus` for the same
# job; we inline it to avoid adding a `urlencoding` crate dependency).
urllib_urlencode
percent_encode

# Approved 2026-05-28: module-level bindings from powerline/segment.py
# at py:225 and py:227. Python declares `always_true = lambda ...` and
# `get_fallback_segment = {...}.copy` — both are module-level
# bindings, not `def`-defined functions. Rust exposes both as
# functions of the same name (`always_true` returns true; `get_fallback_segment`
# returns a fresh fallback dict). Names match Python identifiers
# byte-for-byte. The dict-template + `.copy` callable factory is one
# logical operation in Python; the Rust fn is the same operation.
always_true
get_fallback_segment

# Approved 2026-05-28: Rust helper replicating Python's
# `os.path.ismount(path)`. Used by generate_directories in
# powerline/lib/vcs/__init__.py:19 to terminate the parent-walk at a
# mount boundary. Rust's std::path has no mount-detection primitive;
# the helper compares device IDs via os::unix::fs::MetadataExt::dev,
# which is what os.path.ismount does internally on Unix. Two cfg
# variants (unix + non-unix) — both share the same name.
is_mount

# Approved 2026-05-28: accessor + methods for the version-matching
# subsystem in powerline/bindings/config.py:22-32. Python declares
# CONFIG_FILE_NAME as a module-level compiled regex, CONFIG_MATCHERS
# as a dict of lambdas (None / 'plus' / 'minus'), and CONFIG_PRIORITY
# as a parallel dict. Rust folds the dispatch into a `ConfigMatcher`
# enum with `applies()` and `priority()` methods. `CONFIG_FILE_NAME`
# matches the Python identifier byte-for-byte (lazy-init accessor fn).
# `applies` and `priority` are method names on the enum that replace
# the dict-of-lambdas indirection — same observable semantics.
CONFIG_FILE_NAME
applies
priority

# Approved 2026-05-29: module-level bindings from powerline/bindings/wm/__init__.py:56,70.
# XRANDR_OUTPUT_RE is a Python module-level pre-compiled regex; Rust exposes
# it as a lazy-init accessor fn with the same name byte-for-byte.
# wm_threads is a module-level dict mapping WM name → AwesomeThread class;
# Rust exposes it as an accessor fn returning a HashMap so the lookup shape
# matches Python's `wm_threads['awesome']`.
XRANDR_OUTPUT_RE
wm_threads

# Approved 2026-05-29: name() accessor on StrFunction enum in
# powerline/commands/config.py:9-18. Python's `class StrFunction` has
# a `__str__` method returning `self.name`. Rust enums can't define
# `name()` as a method that mirrors Python's `__str__` AND also be in
# the allowlist (`__str__` isn't a Python fn name; `name` is just an
# attribute). The accessor mirrors Python's `self.name` attribute read
# without inventing new identifier semantics. Also: TMUX_ACTIONS,
# SHELL_ACTIONS, and the two action-lookup helpers
# (tmux_action_from_name, shell_action_from_name) map onto Python's
# module-level dict-based dispatch — Rust splits the lookup into named
# fns since enum matching is fn-based rather than dict-based.
name
TMUX_ACTIONS
SHELL_ACTIONS
tmux_action_from_name
shell_action_from_name

# Approved 2026-05-29: replacement signatures for the Python self.run
# dispatch model in powerline/lib/threaded.py:19. Python's
# MultiRunnedThread.start() calls self.run() — a method overridden by
# subclasses — without the caller having to pass a closure. Rust can't
# model that indirection without trait objects + extra plumbing, so the
# Rust port collapses start() into start_with(closure) (same observable
# semantics) and exposes set_shutdown() so callers don't have to lock
# self.shutdown_event manually. Both names are documented Rust-only
# adapters per the upstream comment block in the file header.
start_with
set_shutdown

# Approved 2026-05-29: key_sep accessor fn for the module-level JStr
# constant from powerline/lint/context.py:16. Python declares
# `key_sep = JStr('/')` at module scope; Rust exposes it as an accessor
# fn so callers can write `key_sep().join(...)` mirroring Python's
# `key_sep.join(...)` shape. Name matches Python's identifier
# byte-for-byte.
key_sep

# Approved 2026-05-29: accessor fn for the class-level dict
# `character_translations` defined on every shell renderer subclass
# (powerline/renderers/shell/{readline,zsh,ksh,bash,tcsh}.py:11-15).
# Python uses class-level dict inheritance + .copy() at class scope;
# Rust exposes the per-class diff as a function returning a fresh
# HashMap on each call. Name matches Python's class-attribute name
# byte-for-byte.
character_translations

# Approved 2026-05-29: render_prefix accessor for the Python override
# `KshPromptRenderer.render()` body which prepends '\001\r' before
# delegating to super(). Rust can't model Python's super() chain
# without trait dispatch + a real ShellRenderer trait; until that
# substrate lands the prefix is exposed as data that callers prepend
# explicitly. Documented Rust-only adapter per PORT.md Rule 0.
render_prefix

# Approved 2026-05-29: accessor fn for the module-level compiled regex
# NERD_TREE_RE from powerline/matchers/vim/plugin/nerdtree.py:10.
# Python: `NERD_TREE_RE = re.compile(b'NERD_tree_\\d+')`.
# Rust exposes it as a lazy OnceLock-backed fn matching the Python
# identifier byte-for-byte.
NERD_TREE_RE

# Approved 2026-05-29: accessor fn for the module-level frozen set
# FINDERS_WITHOUT_PATH from powerline/segments/vim/plugin/commandt.py:58.
# Python: `FINDERS_WITHOUT_PATH = set((...))` — module-level set
# of finder names. Rust exposes it as a lazy OnceLock-backed
# HashSet accessor with the same name byte-for-byte.
FINDERS_WITHOUT_PATH

# Approved 2026-05-29: test-driveable variant of workspace_lister from
# powerline/listers/i3wm.py:23. Python's `workspace_lister` calls
# `get_i3_connection().get_workspaces()` inline, making the filter
# logic impossible to test without a live i3 connection. Rust splits
# the data-supplier from the filter logic so the filter (only_show +
# output match) is independently testable. Same observable semantics
# as Python when called via workspace_lister().
workspace_lister_for

# Approved 2026-05-29: test-driveable variants of tablister/bufferlister
# from powerline/listers/vim.py:28,77. Python calls vim.tabpages /
# vim.buffers inline, making the prefix-and-multiplier logic
# impossible to test without a live vim. Rust splits the data-supplier
# from the logic so the filter (only_show + modified prefix + show_unlisted
# rule + priority multiplier formula) is independently testable. Same
# observable semantics as Python when called via tablister/bufferlister().
tablister_for
bufferlister_for

# Approved 2026-05-29: extraction helper for the inline command assembly
# in powerline/bindings/lemonbar/powerline-lemonbar.py:23. Python inlines
# the command list construction directly in the for-loop body; Rust
# factors it into a testable function so the geometry format string
# `{w}x{h}+{x}+{y}` can be unit-tested without spawning subprocess.
build_bar_command

# Approved 2026-05-29: accessor fn for the module-level dict exit_codes
# from powerline/segments/shell.py:24-28. Python builds it via:
# `dict((k, v) for v, k in reversed(sorted(signal.__dict__.items()))
#   if v.startswith('SIG') and not v.startswith('SIG_'))`
# Rust exposes it as a match-fn over the cross-Unix signal number subset
# (SIGHUP/SIGINT/SIGQUIT/SIGILL/SIGABRT/SIGFPE/SIGKILL/SIGSEGV/SIGPIPE/
# SIGALRM/SIGTERM) — same observable mapping for all signals that
# matter to a shell exit-code (status - 128) lookup.
exit_codes

# Approved 2026-05-29: encoding helper for the LemonbarPowerline class
# from powerline/lemonbar.py:13. Python: `get_encoding = staticmethod(
# lambda: 'utf-8')` — a class-level lambda returning the encoding name.
# Rust exposes it as an associated fn with the same name byte-for-byte.
get_encoding

# Approved 2026-05-29: Rust-only helpers for the inline regex usage in
# powerline/segments/common/mail.py:45-46. Python inlines the regex
# compilation + extraction directly in compute_state's body; Rust
# splits into unseen_re() (lazy-init OnceLock<Regex>) and
# parse_unseen_count() (testable group-extraction) so the parsing
# logic can be unit-tested without spawning an IMAP connection.
unseen_re
parse_unseen_count

# Approved 2026-05-29: Rust-only helpers for the inline syscall usage in
# powerline/segments/common/sys.py:16-72. Python imports cpu_count from
# multiprocessing and inlines os.getloadavg() in the system_load body.
# Rust splits them into:
# - _cpu_count(): wraps std::thread::available_parallelism (mirrors py:6
#   `from multiprocessing import cpu_count as _cpu_count`)
# - _getloadavg(): wraps libc::getloadavg (mirrors py:52 `os.getloadavg()`)
# - render_load_format(): testable {avg:.Nf} format-string substitution
#   for the py:60 `format.format(avg=avg)` call
_cpu_count
_getloadavg
render_load_format

# Approved 2026-05-29: accessor fn for the module-level compiled regex
# NON_PRINTABLE_RE from powerline/lint/markedjson/error.py. Imported by
# reader.py:6 and used in check_printable at py:87-94. Rust exposes
# it as a lazy OnceLock-backed accessor fn matching the Python
# identifier byte-for-byte.
NON_PRINTABLE_RE

# Approved 2026-05-29: format_strftime helper wrapping libc::strftime
# and libc::localtime_r. Python inlines `nw.strftime(format)` directly
# in date()'s body (py:26). Rust splits out the FFI dance into a
# testable helper since calling libc::strftime requires 4 unsafe steps
# (zeroed tm, localtime_r, CString conversion, strftime buffer).
format_strftime

# RichMark::to_string_marked is the Rust analog of Python's
# Mark.to_string() (py:105) in lint/markedjson/error.py. The bare name
# `to_string` collides with Rust's std::string::ToString::to_string
# trait method, so the port appends `_marked` to disambiguate while
# keeping a 1:1 mapping to the Python method. The Display impl
# delegates to to_string_marked(0, "in ", true) so callers still get
# Python-equivalent `format!("{}", mark)`.
to_string_marked

# resolver_singleton is a OnceLock cache for the BaseResolver
# instance, equivalent to Python's `Resolver` class object created
# once at module-load time after the four module-level
# add_implicit_resolver() calls (py:108-127 in lint/markedjson/
# resolver.py). Rust port can't run that init at module-load time so
# the singleton helper provides the same effect: lazy, deterministic
# initialization on first call.
resolver_singleton

# resolver_exact_paths and resolver_prefix_paths field tracking on
# BaseResolver is omitted because the JSON-only loader never
# populates yaml_path_resolvers; the field-bearing methods
# (descend_resolver / ascend_resolver / check_resolver_prefix) port
# in a later pass when yaml_path_resolvers is actually wired up.

# register_defaults runs the four module-level
# add_implicit_resolver() calls from resolver.py:108-127 inside
# BaseResolver::new(). Python executes those at module import time
# against the class object; Rust has no equivalent module-init hook
# for instance state, so the constructor calls the helper to populate
# the per-instance resolver table.
register_defaults

# ComposedNode::node and ComposedNode::start_mark are accessors on
# the Rust ComposedNode sum type (enum wrapping ScalarNode /
# SequenceNode / MappingNode) that the composer uses to descend into
# the shared Node base. Python doesn't need these because the three
# node classes share a Python base class; Rust uses an enum and so
# needs explicit projection accessors.
node
start_mark

# AnyEvent::kind returns the EventKind discriminant for an AnyEvent
# sum-type instance. Python check_event(*choices) passes class
# references and uses isinstance(); the Rust trait passes EventKind
# values and uses an enum match, so .kind() is the projection.
kind

# marked_any_to_json projects a MarkedAny sum-type back to its
# serde_json::Value. Python doesn't need this because the marked
# subclasses (str/int/float/dict/list) inherit from their builtin
# parents directly, so Python code passes them around as-if-Value.
# Rust's MarkedAny is a closed enum and needs explicit projection
# for the construct_yaml_seq / construct_yaml_map re-encode paths.
marked_any_to_json

# composed_id returns the 'scalar'/'sequence'/'mapping' identifier
# used by ConstructorError messages. Python relies on `node.id` —
# a class attribute defined on each Node subclass (ScalarNode.id =
# 'scalar', etc., at nodes.py:32/49/53). Rust ComposedNode is an
# enum, so projecting that identifier requires an explicit helper
# rather than attribute access.
composed_id

# AnyToken accessor methods (token_kind / token_start_mark /
# token_end_mark / token_id) project per-variant fields out of the
# Rust AnyToken sum. Python uses class references + duck-typed
# attribute access (`token.start_mark`, `token.id`, etc.) which
# resolves polymorphically over the upstream Token class hierarchy.
# Rust port uses an enum and so requires explicit projection
# accessors. The `token_` prefix avoids collision with composer.rs
# AnyEvent's `.kind()`/`.start_mark()` accessors already in the
# allowlist.
token_kind
token_start_mark
token_end_mark
token_id

# Parser::dispatch_state is the Rust analog of Python's
# `self.state()` bound-method-pointer dispatch (py:31, 41-43, 48-50
# in parser.py). Python stores a method reference in self.state and
# invokes via direct call; Rust port stores a State enum and
# dispatches via a match table inside dispatch_state. The function
# itself doesn't exist in upstream — it materialises the indirection
# Python gets for free from method-as-value semantics.
dispatch_state

# TmuxRenderer::color_spec is a helper extracting the repeated
# fg/bg color-emission logic at py:47-52 (fg) and py:54-59 (bg) of
# renderers/tmux.py. Python inlines both blocks; Rust deduplicates
# them into one private fn that takes the channel name ("fg"/"bg")
# and the ColorSpec, then emits the appropriate
# "default" / "#rgb" / "colourN" form based on term_truecolor and
# the truecolor option presence.
color_spec

# VimRenderer::mode_translations builds the Python module-level
# `mode_translations` dict from renderers/vim.py:20-23 as a Rust
# accessor fn. Python initialises the dict at import time;
# Rust needs an explicit constructor since module-level mutable
# globals don't carry the same semantics.
mode_translations

# HlGroup::vim_command builds the ":hi <name> ctermfg=... guifg=..."
# command string that Python issues via `vim.command(...)` at
# renderers/vim.py:174. Python inlines that string-format call
# inside hlstyle(); Rust extracts it onto HlGroup so the caller can
# queue the command for batch dispatch (Python's vim.command isn't
# reachable from Rust without a vim binding).
vim_command

# VimRenderer::build_group_name materialises the synthetic
# Pl_<ctermfg>_<guifg>_<ctermbg>_<guibg>_<attrs> name (py:165-172).
# Python inlines this in hlstyle; Rust extracts to keep the method
# body legible (the field assembly + str() coercion would obscure
# the cache check around it otherwise).
build_group_name

# VimRenderer::attrs_to_hi_attrs builds the vim-format attrs name
# list from a powerline ATTR_* bitfield (py:157-163). Python
# inlines the if-attrs branch inside hlstyle; Rust extracts to a
# helper so tests can pin the bit→name mapping in isolation.
attrs_to_hi_attrs

# FileStatus::as_char projects the Rust FileStatus enum back to the
# single-character display string Python obtains via dict tuple
# indexing (self.statuses[code][0]) at lib/vcs/mercurial.py:69 and
# elsewhere. Python's tuple-as-record pattern collapses into Rust
# enum-with-accessor; the projection has to live somewhere.
as_char

# statuses() is the Rust accessor for Repository.statuses (py:26-28).
# Python defines it as a class attribute initialised at class-body
# evaluation time. Rust uses OnceLock for the same lazy single
# initialisation pattern.
statuses

# aggregate_repo_status extracts the bitmask aggregation loop at
# lib/vcs/mercurial.py:79-82 into a testable static method. Python
# inlines the loop inside do_status; Rust extracts so the bit→string
# mapping can be unit-tested in isolation without needing the hg
# shell-out (which is stubbed in this port pass).
aggregate_repo_status

# nick_pat() is the Rust accessor for the Python module-level
# `nick_pat = re.compile(...)` constant at lib/vcs/bzr.py:23.
# Python initialises module-level regex at import time; Rust
# uses OnceLock for the same lazy single-init pattern.
nick_pat

# Repository::aggregate_short_status extracts the loop at
# lib/vcs/bzr.py:87-93 that walks the `bzr status -S` raw output
# and aggregates per-line indicators into the "DU"/"D "/" U"/None
# repo-status string. Python inlines this inside _status(); Rust
# extracts so the line→aggregate mapping can be unit-tested
# without needing the bzrlib shell-out (which is stubbed).
aggregate_short_status

# Repository::extract_file_status extracts the per-file branch at
# lib/vcs/bzr.py:80-83 (`ans = raw[:2]; if ans == 'I ': ans = None`).
# Python inlines this inside _status when path is provided; Rust
# extracts for the same testability reason as aggregate_short_status.
extract_file_status

# _ref_pat() accessor for the Python module-level
# `_ref_pat = re.compile(...)` constant at lib/vcs/git.py:15.
# Python initialises module-level regex at import time;
# Rust uses OnceLock for the same lazy single-init pattern.
_ref_pat

# Repository::aggregate_porcelain_status extracts the
# wt_column/index_column/untracked_column aggregation loop at
# lib/vcs/git.py:194-208 (shell-out backend) and the equivalent
# py:141-159 pygit2 branch. Python inlines the loop inside
# do_status; Rust extracts so the line→aggregate mapping can be
# unit-tested without needing the git shell-out (which is stubbed
# in this port pass).
aggregate_porcelain_status

# which_exists is a private $PATH lookup helper. Python uses
# powerline.lib.shell.which (py:12 import). Rust port can't yet
# call the unported shell module so this duplicates the
# functionality locally — when lib/shell.rs gains which(), this
# will move there. Allowlist entry captures the
# scaffolding-helper status.
which_exists

# used_styles() and seen() are Rust accessors for the Python
# module-level `used_styles = []` (py:16) and `seen = set()` (py:17)
# globals in renderers/ipython/since_7.py. Python mutates these at
# import + hl() call time; Rust uses OnceLock<Mutex<...>> for the
# equivalent thread-safe lazy-init / mutation pattern.
used_styles
seen

# IPythonPygmentsRenderer::attrs_to_attr_names builds the
# bold/italic/underline name list from a powerline ATTR_* bitfield
# per renderers/ipython/since_7.py:55-60. Python inlines the
# if-attrs branch inside hl(); Rust extracts to a helper so the
# bit→name mapping can be unit-tested in isolation.
attrs_to_attr_names

# IPythonPygmentsRenderer::build_token_name and build_style_rule
# materialise the Python inline `name = 'pl' + ...` and
# `' '.join(' ' + a) + ' fg:#'+fg` builders at
# renderers/ipython/since_7.py:62-77. Rust extracts to helpers so
# the synthetic name format + style-rule format can be pinned with
# focused tests, separating them from the global-mutation logic in
# hl(). Without extraction the global-state side effects would
# require complex setup to exercise the name/rule format.
build_token_name
build_style_rule

# WithPath::exit is the Rust analog of Python's `__exit__` dunder
# method at lint/imp.py:17. Python exposes the context-manager
# protocol via `__enter__`/`__exit__`; Rust drops the leading
# underscores since they're reserved for the language's own attr
# magic (and `__exit__` isn't a valid Rust identifier anyway).
# `enter` already exists in upstream as a non-dunder match;
# `exit` does not — the only upstream variant is `__exit__`.
exit

# PromptKind::cache_key and PromptKind::matcher_info are
# accessor methods on the Rust PromptKind enum. Python loops
# `for prompt in ('in', 'continuation', 'rewrite', 'out'):` and
# uses the loop variable directly as both the cache key and the
# matcher_info ternary input (py:74, 76 in bindings/ipython/
# since_5.py). Rust represents the same discrimination as an enum
# so the projection has to live somewhere; both accessors are
# pure projections of the enum variant.
cache_key
matcher_info

# PowerlinePrompts::prompt_tokens is the Rust analog of the four
# `<name>_prompt_tokens` methods Python generates via `exec` at
# bindings/ipython/since_5.py:65-78. Python's exec-generated
# method names ({prompt}_prompt_tokens) don't appear in the static
# function-name corpus because the generation happens at class-body
# eval time. The Rust port consolidates them into one parameterised
# `prompt_tokens(kind, render)` method + four named convenience
# wrappers (in_prompt_tokens / continuation_prompt_tokens /
# rewrite_prompt_tokens / out_prompt_tokens) preserving the
# upstream call sites.
prompt_tokens
in_prompt_tokens
continuation_prompt_tokens
rewrite_prompt_tokens
out_prompt_tokens

# old_prompt_manager() is the Rust accessor for the Python
# module-level `old_prompt_manager = None` global at
# bindings/ipython/post_0_11.py:36. Python initialises this at
# import time and mutates inside do_setup/unload_ipython_extension;
# Rust uses OnceLock<Mutex<Option<Value>>> for the equivalent
# thread-safe lazy-init / mutation pattern.
old_prompt_manager

# ShutdownHook::set_powerline materialises the Python attribute
# assignment `shutdown_hook.powerline = ref(self)` at
# bindings/ipython/post_0_11.py:97 as an explicit method since
# Rust struct fields aren't mutated by external attribute syntax.
set_powerline

# ShutdownHook::call is the Rust analog of Python's `__call__`
# dunder at bindings/ipython/post_0_11.py:44. Rust drops the
# leading dunder underscores since they're reserved for the
# language's own attr magic.
call

# rspace() is the Rust accessor for the Python module-level
# `rspace = re.compile(r'(\s*)$')` at
# bindings/ipython/pre_0_11.py:57. Python initialises the regex at
# class-body eval time; Rust uses OnceLock<Regex> for the
# equivalent lazy single-init pattern.
rspace

# PowerlinePrompt1::to_p_str is the Rust analog of Python's
# `__str__` dunder at bindings/ipython/pre_0_11.py:57. Rust drops
# the leading dunder underscores since they're reserved for the
# language's own attr magic; `to_p_str` describes the body
# behaviour (it builds and returns `self.p_str`).
to_p_str

# PowerlineTextBox::with_defaults() returns a fresh instance using
# the default values from Python's keyword-argument signature at
# bindings/qtile/widget.py:18 (timeout=2, text=b' ',
# width=CALCULATED, side='right', update_interval=None). Python
# supports calling `PowerlineTextBox()` with no args; Rust requires
# explicit defaults, so this helper materialises them. The
# `new()` constructor takes the full positional args matching the
# upstream kwargs.
with_defaults

# WORKSPACE_REGEX() is the Rust accessor for the Python module-level
# WORKSPACE_REGEX = re.compile(...) at segments/i3wm.py:9.
# Python initialises the regex at module-load; Rust uses OnceLock
# for the equivalent lazy single-init pattern.
WORKSPACE_REGEX

# ws_icons() and scratchpad_icons() are the Rust accessors for the
# Python module-level WS_ICONS = {...} and SCRATCHPAD_ICONS = {...}
# dict constants at segments/i3wm.py:39 and py:272-274. Python
# initialises the dicts at module-load time and merges them into
# user-supplied icon maps; Rust returns a fresh Map each call so
# the merge step at py:42-44 doesn't mutate a shared static.
ws_icons
scratchpad_icons

# mode_segment is the Rust analog of the @requires_segment_info
# `mode` segment function at segments/i3wm.py:243. The bare name
# `mode` would collide with Rust enum variants / common identifiers
# (e.g. crate::ported::theme::Mode), so the port appends `_segment`
# to disambiguate while preserving the Python function's contract.
mode_segment

# active_window_contents extracts the cutoff-vs-window_class
# decision from segments/i3wm.py:295-302 into a pure helper
# (title length > cutoff → window_class, else title). Python
# inlines this inside the segment function; Rust extracts so the
# decision can be unit-tested without the i3ipc connection.
active_window_contents

# build_workspace_entry extracts the format-string substitution
# loop from segments/i3wm.py:140-150 (workspaces segment body)
# into a pure helper. Python uses str.format with keyword args;
# Rust does explicit replace() calls so the substitution can be
# tested without the live i3ipc tree.
build_workspace_entry

# cwd_segments materialises the Rust analog of CwdSegment.__call__
# at segments/common/env.py:74. The bare `__call__` dunder isn't a
# valid Rust identifier so the port renames to `cwd_segments` to
# describe the result (a list of breadcrumb segments). Python emits
# this via `cwd = with_docstring(CwdSegment(), '''...''')` at py:107
# which makes the instance the documented public API; Rust users
# call the free function directly.
cwd_segments

# external_ip_render is the Rust analog of
# ExternalIpSegment.render() at segments/common/net.py:51. Python
# defines it as an instance method on the ThreadedSegment subclass;
# Rust exposes it as a free function since the threading framework
# is unported and the render logic is pure (ip → segment dict).
external_ip_render

# interface_starts() materialises the Python module-level
# `_interface_starts = {...}` dict at segments/common/net.py:79-91
# as a Rust accessor returning the ordered (prefix, priority) slice.
# Python initialises the dict at module-load; Rust uses a static
# array since the lookup needs preserve insertion order (Python's
# dict iteration order matches insertion order).
interface_starts

# _interface_start_re() is the Rust accessor for the Python
# module-level _interface_start_re = re.compile(...) at
# segments/common/net.py:93. OnceLock for the same lazy-init
# pattern used across all module-level regex ports.
_interface_start_re

# replace_num_pat() is the Rust accessor for the Python
# NetworkLoadSegment.replace_num_pat class attribute at
# segments/common/net.py:191. Class-attribute init becomes
# module-level OnceLock since the segment class itself is stubbed.
replace_num_pat

# weather_conditions_codes() and weather_conditions_icons() are
# Rust accessors for the Python module-level
# `weather_conditions_codes = {...}` and `weather_conditions_icons
# = {...}` dicts at segments/common/wthr.py:21-77 and py:79-92.
# Python initialises the dicts at module-load; Rust uses OnceLock
# for the equivalent lazy single-init pattern.
weather_conditions_codes
weather_conditions_icons

# temp_conversions() and temp_units() are the Rust analogs of the
# Python module-level dict-with-lambdas at segments/common/wthr.py:
# 94-98 and py:101-105. Python uses `{'C': lambda t: t-273.15,
# ...}`; Rust can't store lambdas in a HashMap by name without
# boxing, so the port surfaces them as match-based functions
# keyed by the unit string.
temp_conversions
temp_units

# weather_key() is the Rust analog of WeatherSegment.key() at
# segments/common/wthr.py:113 (a @staticmethod that constructs the
# _WeatherKey namedtuple). The bare `key` name would collide with
# common identifiers (HashMap key arg, etc.); the port prepends
# `weather_` for disambiguation.
weather_key

# compute_state_from_response extracts the JSON-parse pure path
# from WeatherSegment.compute_state at segments/common/wthr.py:
# 141-160. Python mixes the urllib_read network call with the
# JSON parsing in a single method; Rust separates them so the
# parse logic can be unit-tested without a network stub.
compute_state_from_response

# pick_icon extracts the for/else icon-selection block at
# segments/common/wthr.py:171-176 into a pure helper. Python uses
# the for/else idiom which has no direct Rust analog; the helper
# inlines the equivalent walk + fallback in straight-line code.
pick_icon

# temp_gradient_level extracts the 3-branch gradient computation
# at segments/common/wthr.py:181-187 (clamp coldest to 0, clamp
# hottest to 100, linear interpolation otherwise) into a pure
# helper so the boundary behaviour can be unit-tested without the
# render_one segment-list scaffolding.
temp_gradient_level

# BATTERY_PERCENT_RE() is the Rust accessor for Python's
# `BATTERY_PERCENT_RE = re.compile(r'(\d+)%')` at
# segments/common/bat.py:146. Python initialises the regex inside
# a closure scope at module-load; Rust uses OnceLock for the same
# lazy-init pattern.
BATTERY_PERCENT_RE

# parse_pmset_output extracts the pmset-output parsing inlined at
# segments/common/bat.py:148-151 (regex search + AC substring check)
# into a pure helper. Python keeps it inside the closure that
# captures `pl`; Rust separates so the parse can be tested without
# the shell-out dispatch.
parse_pmset_output

# parse_linux_status extracts the /sys/class/power_supply status
# string parse at segments/common/bat.py:111 into a pure helper.
# Python inlines `state &= (line.strip() != 'Discharging')`; Rust
# extracts so the discharging/charging classification can be tested
# without the file I/O setup.
parse_linux_status

# flatten_battery aggregates the per-device energy/energy_full/state
# loop at segments/common/bat.py:54-77 (DBUS+UPower) and the
# equivalent /sys/class/power_supply loop at py:101-114 into a single
# helper. Python inlines the loop inside the closure each backend
# constructs; Rust extracts so the aggregation can be tested
# independently of the live battery-detection dispatch.
flatten_battery

# build_device_table constructs the per-device tuple list from
# parallel slices (Rust zip-based) — analog of the Python `devices`
# list-of-tuples accumulated inside the /sys/class/power_supply
# detection loop at segments/common/bat.py:99-101. Helper exists so
# callers wiring real `/sys/class/power_supply/*` reads can build
# the input to flatten_battery without duplicating the zip dance.
build_device_table

# state_symbols() is the Rust accessor for the Python
# module-level STATE_SYMBOLS = {...} dict at
# segments/common/players.py:12-17. Returns a fresh Map per call
# since callers may mutate the dict locally before passing to
# player_segment_call (Python's mutable default arg pattern).
state_symbols

# _convert_seconds_str is the str-input variant of
# _convert_seconds matching the isinstance(seconds, str) branch
# at segments/common/players.py:33. Python overloads the type;
# Rust splits into separate parse-then-convert functions since
# Rust doesn't support runtime type dispatch on the argument.
_convert_seconds_str

# player_segment_call materialises PlayerSegment.__call__ at
# segments/common/players.py:40. The bare `__call__` dunder isn't
# a valid Rust identifier so the port renames to
# player_segment_call to describe the result (build the player
# segment list).
player_segment_call

# INotifyFileWatcher::now is the Rust analog of Python's
# `monotonic()` import from powerline.lib.monotonic at
# lib/watcher/inotify.py:11. Python calls it as a free function;
# Rust extracts to an associated static method for self-contained
# struct API.
now

# query is the Rust analog of Python's __call__ dunder at
# lib/watcher/inotify.py:105 (file watcher) and py:266 (tree
# watcher). Rust drops the leading dunder underscores since
# they're reserved for language attribute magic; query() name
# describes the side-effect-free + returns-current-state contract.
query

# mark_modified is the Rust analog of Python's `self.modified[path]
# = True` attribute assignment inside process_event at
# lib/watcher/inotify.py:65. Rust struct fields can't be mutated
# via external attribute syntax, so the port surfaces this as a
# mutator method the caller (event loop) invokes when it processes
# inotify events.
mark_modified

# pycmd() is the Rust accessor for Python's module-level
# `pycmd = None` global at powerline/vim.py:340. Python initialises
# the global and mutates it via set_pycmd; Rust uses OnceLock<Mutex>
# for the same lazy-init + mutable global pattern.
pycmd

# create_window_statusline_format extracts the closure-returning
# function-body at powerline/vim.py:74-79 (the Python-3 branch of
# create_window_statusline_constructor). Python wraps the bytes
# concat in a lambda; Rust returns an impl-Fn(u64) -> String. The
# bare name `create_window_statusline_constructor` is in the upstream
# corpus but the Rust port renames to clarify the result is a format
# template, not a constructor.
create_window_statusline_format

# get_matcher_module materialises the rpartition('.') dispatch at
# powerline/vim.py:185-188 (VimPowerline.get_matcher body) into a
# pure helper. Python inlines the rpartition; Rust extracts so the
# (module, function) split can be unit-tested without the
# get_module_attr / matcher_generator dispatch chain.
get_matcher_module

# failed_unicode_message surfaces the Python
# `FailedUnicode('No window {0}'.format(window_id))` constructor
# call at powerline/vim.py:303 as a pure helper. Python returns a
# FailedUnicode (subclass of str); Rust just returns the formatted
# String since the FailedUnicode wrapper class is unported.
failed_unicode_message

# setup_entry is the Rust analog of the module-level `setup()`
# function at powerline/vim.py:354. The bare name `setup` collides
# with Rust's setup-related conventions (Cargo build script
# semantics, test setup methods, etc.), so the port renames to
# disambiguate while preserving the Python module-level entry point.
setup_entry

# used_powerlines() is the Rust accessor for Python's module-level
# `used_powerlines = WeakValueDictionary()` at
# bindings/zsh/__init__.py:19. Python's WeakValueDictionary auto-
# removes entries when the value is GC'd; Rust has no weakref in
# std so the port uses HashSet<u64> of ids, with explicit
# add/remove from do_setup/shutdown.
used_powerlines

# Args::ext is the Rust accessor for the Python class attribute
# `ext = ['shell']` at bindings/zsh/__init__.py:41. Python defines
# it as a class attribute; Rust surfaces it as a static method
# returning a fresh Vec since &[&str] would need a 'static
# lifetime annotation that breaks the drift gate brace counter.
ext

# config_path_from_var extracts the @property body at
# bindings/zsh/__init__.py:52-66 (Args.config_path) into a pure
# helper. Python inlines the zsh.getvalue + split('/') + filter
# logic inside the property; Rust extracts so the split semantics
# can be unit-tested with a value parameter (rather than the live
# zsh.getvalue call).
config_path_from_var

# Environment::contains is the Rust analog of Python's
# __contains__ dunder at bindings/zsh/__init__.py:93. Rust drops
# the leading dunder underscores since they're reserved for
# language attribute magic.
contains

# set_prompt_zpyvar_name extracts the py:201
# `zpyvar = 'ZPYTHON_POWERLINE_' + psvar` builder from set_prompt
# into a pure helper. Python embeds it inside the function body
# that also calls zsh.set_special_string; Rust extracts so the
# variable-name format can be tested in isolation.
set_prompt_zpyvar_name

# build_segment_info extracts the dict-literal segment_info builder
# at bindings/zsh/__init__.py:158-167 (Prompt.__str__) into a pure
# helper. Python builds it inline inside the __str__ method;
# Rust extracts so the per-field semantics can be tested without
# the zsh.expand/zsh.getvalue dispatch around it.
build_segment_info

# ZshPowerline::next_id is the Rust analog of Python's id(self)
# call at bindings/zsh/__init__.py:131 used as the WeakValueDictionary
# key. Python's id() returns the object's memory address; Rust
# uses a static OnceLock<Mutex<u64>> counter for the equivalent
# process-unique identifier.
next_id

# np_control_character_translations / np_invalid_character_translations
# / np_invalid_character_re / np_character_translations are Rust
# accessors for the Python module-level constants at
# powerline/renderer.py:18-67. Python initialises them at module-load;
# Rust uses OnceLock for the same lazy-init pattern. np_character_
# translations returns a fresh HashMap because the union is computed
# from two OnceLock tables and Rust prefers explicit clone semantics.
np_control_character_translations
np_invalid_character_translations
np_invalid_character_re
np_character_translations

# translate_np is the Rust analog of the module-level translate_np
# function-or-lambda at powerline/renderer.py:68-82. Python ports
# the Python-version split (UCS-2 vs UCS-4) into a runtime dispatch;
# Rust port runs the UCS-4 path always since Rust chars are full
# unicode codepoints.
translate_np

# width_data() is the Rust accessor for the Python module-level
# width_data dict at powerline/renderer.py:177-184. Python sets it
# as an instance attribute inside __init__; Rust extracts to a free
# function that takes the ambiwidth arg, so callers can build the
# table without instantiating the full Renderer.
width_data

# Renderer::segment_info is a static class attribute at
# powerline/renderer.py:124-128 (returns the {environ, getcwd,
# home} dict). Python defines it inline in the class body; Rust
# extracts to an associated function since the class-body dict
# initialisation runs at import time and reads from os.environ.
segment_info

# hexdigits_set() is the Rust accessor for Python's
# `hexdigits_set = set(hexdigits)` module-level constant at
# lint/markedjson/scanner.py:11. Python initialises the set at
# import time from the stdlib `string.hexdigits` constant; Rust
# uses OnceLock for the same lazy single-init pattern.
hexdigits_set

# dispatch_fetch_for is the Rust analog of Python's chain of
# `if ch == '...': return self.fetch_...()` calls inside
# fetch_more_tokens at lint/markedjson/scanner.py:140-187. Python
# inlines the dispatch and immediately invokes each branch on
# `self`; Rust extracts the (char, flow_level) → FetchKind
# discrimination into a pure function so it can be tested
# without scaffolding the full Scanner+Reader state machine.
# The Scanner methods (fetch_stream_end / fetch_flow_collection_*)
# are still ported separately as named impls.
dispatch_fetch_for

# CheckResult::ok / CheckResult::failed are Rust analogs of the
# (proceed, hadproblem) tuple Python check_*/match methods return
# at powerline/lint/spec.py:194/219/etc. Python builds the tuple
# inline; Rust extracts to constructor methods so callers don't
# have to repeat the boolean pattern.
ok
failed

# Spec::type_check is the Rust analog of Spec.type at
# powerline/lint/spec.py:379. The bare name `type` is a Rust
# reserved keyword (used for type aliases), so the port renames
# to `type_check` to preserve the chainable-builder semantics
# while staying syntactically valid.
type_check

# Spec::regex is the Rust analog of Spec.re at
# powerline/lint/spec.py:552. The bare name `re` collides with
# the Rust `regex` crate's `Regex::re` import shape (and is
# overloaded as a short word in Rust code); the port renames to
# `regex` to disambiguate while preserving the chainable-builder
# call site shape.
regex

# generic_keys() / type_keys() / required_keys() / highlight_keys()
# are Rust accessors for the Python module-level set/dict constants
# at powerline/lint/checks.py:24-43. Python initialises them at
# import time; Rust uses OnceLock for the same lazy single-init
# pattern.
generic_keys
type_keys
required_keys
highlight_keys

# LintResult::warned is a constructor on the Rust LintResult struct
# (Rust analog of Python's (proceed, echo, hadproblem) tuple
# returned by check_* helpers at lint/checks.py:684). Python
# returns the tuple inline; Rust extracts to constructor methods so
# callers don't repeat the boolean pattern. ok/failed live in
# CheckResult/LintResult under their own allowlist entry already
# (from lint/spec.rs).
warned

# common_names() is the Rust accessor for the Python module-level
# `common_names = defaultdict(set)` at lint/checks.py:755. Python
# initialises and mutates the defaultdict at import time; Rust
# uses OnceLock<Mutex<HashMap>> for the equivalent thread-safe
# mutable global pattern.
common_names

# PowerlineStyleDict::lookup is the Rust analog of Python's
# defaultdict __missing__ + __getitem__ at
# renderers/ipython/since_5.py:35-37. Rust struct fields can't
# trigger lazy initialisation via attribute syntax; the port
# surfaces a lookup(key, missing_func) method so callers
# supply the fallback explicitly. Python's dict[key] indexing
# triggers __missing__ automatically.
lookup

# PowerlineStyleDict::insert wraps HashMap::insert so callers
# don't need to access the inner field directly. Python uses
# dict[key] = value syntax which doesn't translate to Rust;
# the helper exists to preserve the encapsulation boundary.
insert

# powerline_prompt_style_invalidation_hash extracts the Python
# `super().invalidation_hash() + 1` line at
# renderers/ipython/since_5.py:79-80 into a pure function over
# the base hash value. Python relies on dynamic super(); Rust
# port takes the base hash as an argument since there's no
# DynamicStyle base class to inherit from.
powerline_prompt_style_invalidation_hash

# parse_token_attrs extracts the get_attrs_for_token parsing
# loop at renderers/ipython/since_5.py:55-65 into a pure
# function. Python inlines the parse inside the method body
# alongside the Attrs(...) constructor; Rust port separates
# the parse so the synthetic-name decoder can be tested
# without the pygments token-tree dispatch around it.
parse_token_attrs

# WrappedOrigin::wrapper / WrappedOrigin::origin are Rust accessor
# methods on the WrappedOrigin struct, the port's analog of Python's
# attribute assignment `r.powerline_origin = wrapped` at
# lib/__init__.py:9. Python reads the attribute via dot syntax; Rust
# can't attach arbitrary attributes to fn items so the port exposes
# them as struct-field accessor methods.
wrapper
origin

# add_divider_highlight_group_to_list is the list-input variant of
# add_divider_highlight_group at lib/__init__.py:14. Python applies
# the same decorator regardless of whether the wrapped function
# returns a string or a list of segments; Rust splits into two
# functions since the return-type discrimination at runtime needs
# trait dispatch.
add_divider_highlight_group_to_list

# branch_name_cache() / branch_lock() / file_status_lock() /
# file_status_cache() / tree_status_cache() are Rust accessors for
# the Python module-level mutable globals at lib/vcs/__init__.py:
# 45-47, 121, 212 (branch_name_cache, branch_lock, file_status_lock,
# file_status_cache, _tree_status_cache). Python initialises them at
# import time; Rust uses OnceLock<Mutex<...>> for the equivalent
# thread-safe lazy-init + mutable-global pattern.
branch_name_cache
branch_lock
file_status_lock
file_status_cache
tree_status_cache

# _uv_thread() is the Rust accessor for Python's module-level
# `_uv_thread = None` global at lib/watcher/uv.py:57. Python
# initialises it at import time + mutates inside start_uv_thread;
# Rust uses OnceLock<Mutex<Option<bool>>> for the equivalent
# thread-safe lazy-init + mutable-global pattern.
_uv_thread

# UvWatcher::watch_count is a test-friendly accessor for the
# Python `len(self.watches)` introspection at lib/watcher/uv.py.
# Python uses the dict's __len__ directly; Rust requires a Mutex
# lock + len() pair so the helper makes the lock acquisition
# explicit at the type boundary.
watch_count

# UvFileWatcherEvents::record_event is the Rust analog of Python's
# UvFileWatcher._record_event method body at lib/watcher/uv.py:142.
# The Rust port can't reproduce pyuv's FSEvent callback dispatch,
# so the method becomes the caller's manual entry point for
# populating the events buffer (tests + future notify integration
# both use this).
record_event

# UvFileWatcherEvents::event_count is a test-introspection helper
# (Python `len(self.events[path])` would suffice). Same Mutex-lock
# justification as watch_count above.
event_count

# function_name_re() is the Rust accessor for Python's module-level
# `function_name_re = r'^(\w+\.)*[a-zA-Z_]\w*$'` regex pattern at
# lint/__init__.py:43. Python stores it as a plain string constant;
# Rust uses OnceLock<Regex> for the lazy single-compile pattern
# (consistent with other module-level regex ports).
function_name_re

# strip_json_suffix extracts the Python `name[:-5]` suffix-strip
# from lint/__init__.py:361 + py:373 (where ext_name and
# config_file_name lose their '.json' extension before being
# stored as config entry names). Python inlines the slice; Rust
# extracts to a pure helper so the strip-or-passthrough behaviour
# can be tested in isolation.
strip_json_suffix

# vim_modes() is the Rust accessor for Python's module-level
# `vim_modes = {...}` dict at segments/vim/__init__.py:43. Python
# initialises the dict at import time; Rust uses OnceLock<HashMap>
# for the equivalent lazy single-init pattern.
vim_modes

# mode_translation is the Rust analog of the @window_cached
# mode(pl, segment_info, override=None) segment fn at
# segments/vim/__init__.py:92. Python's `mode` collides with the
# Rust reserved-keyword adjacent identifier; the port renames to
# mode_translation to describe the result (a name lookup with
# fallback chain).
mode_translation

# position_value / line_percent_value / visual_range_text /
# file_size_text extract the pure value-computation cores from
# the position / line_percent / visual_range / file_size segment
# fns at segments/vim/__init__.py:415-442, py:394-413, py:120-170,
# py:314. The segment fns themselves call vim.current.buffer +
# vim_funcs['line'] which aren't reachable from Rust; the pure
# helpers extract the math/formatting branches so callers can
# unit-test the data-flow without the vim runtime.
position_value
line_percent_value
visual_range_text
file_size_text

# default_position_strings() returns the Python keyword-default
# `position_strings={'top': 'Top', 'bottom': 'Bot', 'all': 'All'}`
# from segments/vim/__init__.py:415. Python embeds the default in
# the fn signature; Rust extracts to a free helper since Rust
# doesn't support mutable HashMap default-arg semantics.
default_position_strings

# PowerlineLogger::format_message extracts the format string
# building from powerline/__init__.py:46-109 (the message-emission
# helpers debug/info/warn/etc all wrap the same format pattern).
# Python embeds the format inside each per-level method; Rust
# extracts to a pure helper so the {ext}:{prefix}:{message}
# pattern can be unit-tested without dispatching through the
# logger framework.
format_message

# PowerlineLogger::log is the Rust analog of the per-level method
# dispatch at powerline/__init__.py:80-108. Python defines
# critical/exception/info/error/warn/debug as separate methods
# that each call self.logger.log(level, msg); Rust unifies them
# into a single log(level, message) method plus thin convenience
# wrappers since Rust can't reduce dispatch via *args/**kwargs
# the way Python does.
log

# unicode.rs::out_u_str / out_u_bytes split the Python out_u()
# from powerline/lib/unicode.py:106-118 into two type-overloaded
# Rust functions because Python's runtime dispatches on
# isinstance(s, unicode) vs isinstance(s, bytes) at py:113-117.
# Rust requires distinct fn names to encode the &str vs &[u8] type
# split since Rust doesn't have ad-hoc overloads.
out_u_str
out_u_bytes

# unicode.rs::string_from_str / string_from_bytes is the same
# pattern for the Python string() at powerline/lib/unicode.py:162-173.
# Py3 string() returns s unchanged for str input (py:170) and
# s.decode('utf-8') for bytes input (py:171). Rust splits the
# isinstance dispatch into two named functions.
string_from_str
string_from_bytes

# FailedUnicode::as_str is the borrowing accessor on the Rust
# newtype FailedUnicode(String) wrapping the Python unicode
# subclass at powerline/lib/unicode.py:150-159. Python callers
# treat FailedUnicode as a string directly via isinstance + slicing;
# Rust provides as_str() as the canonical &str borrow.
as_str

# FailedUnicode::into_string is the consuming accessor on the
# Rust newtype FailedUnicode(String). Python callers use the
# FailedUnicode instance as a string by virtue of unicode
# inheritance at py:150; Rust requires explicit unwrap to extract
# the inner String, named into_string to mirror std conventions.
into_string

# bindings/config.rs::TMUX_VAR_RE mirrors the Python module-level
# regex constant at powerline/bindings/config.py:179. Python exposes
# the compiled re object as the module-attribute TMUX_VAR_RE; Rust
# exposes it as a fn returning &'static Regex (OnceLock-backed) so
# the same name is used at call sites. Same pattern already approved
# for CONFIG_FILE_NAME above.
TMUX_VAR_RE

# vendor/powerline/scripts/ entry-point ports
#
# The extractor scripts/extract_py_names.py only walks
# vendor/powerline/powerline/ (the Python package). Standalone
# scripts at vendor/powerline/scripts/ aren't scanned, so every
# fn ported from those scripts is flagged here.

# powerline-daemon:33 USE_FILESYSTEM module-level constant —
# Rust exposes as fn returning bool since the value depends on
# target_os which is a compile-time cfg, but we want callers to
# use the same identifier as the Python source.
USE_FILESYSTEM

# powerline-daemon:65 HOME module-level constant —
# Rust exposes as fn returning String since computing
# os.path.expanduser('~') requires a process env read; same
# identifier preserved for source parity with the Python.
HOME

# powerline-daemon:69-70 NonDaemonShellPowerline.get_log_handler
# returns logging.StreamHandler(). Rust mirrors the method on the
# placeholder marker struct so callers exercise the same shape.
get_log_handler

# powerline-daemon:137-144 eintr_retry_call(func, *args, **kwargs)
# loops calling func and retries on EnvironmentError with EINTR.
# Rust port loops the closure with io::ErrorKind::Interrupted.
eintr_retry_call

# powerline-daemon:175-190 safe_bytes(o, encoding) returns bytes
# instance without throwing. Rust port collapses to &str.as_bytes()
# since &str is always valid UTF-8.
safe_bytes

# powerline-daemon:374-385 kill_daemon(address) — connects to the
# daemon's UNIX socket and sends EOF sentinel. Returns true if the
# daemon was reachable. Same identifier preserved.
kill_daemon

# powerline-daemon:355-371 check_existing(address) — tries to bind
# the socket; returns the bound socket (or None) on Python. Rust
# returns bool since callers immediately re-bind in main_loop.
check_existing

# powerline-daemon:388-398 cleanup_lockfile(pidfile, fd, *args)
# atexit + SIGTERM handler that unlinks the pidfile and closes
# the fd. Rust port: unlink only; fd close happens via Drop on the
# File handle.
cleanup_lockfile

# powerline-release.py:18 OVERLAY_BRANCH_FORMAT template constant.
# Python uses str.format(); Rust exposes as fn taking the version
# string for clarity (Rust has no format-string-as-data
# equivalent without macro support).
overlay_branch_format

# powerline-release.py:21-48 parse_version(s) — parses either a
# plain dotted version or a "+++"-style bump request.
# Rust port takes the latest_version array as an explicit arg
# since Python reads it via `check_output(['git', 'tag', '-l'])`.
parse_version

# powerline-release.py:62-65 setup_py_develop_filter(line, version)
# rewrites the `\tbase_version = '...'` line. Pure functional.
setup_py_develop_filter

# powerline-release.py:68-73 setup_py_master_filter(line, version)
# rewrites the `\tversion='...'` line and pins Development Status.
# Pure functional.
setup_py_master_filter

# powerline-release.py:215-221 create_release(version, user, password,
# run_stages) drives the release stages in order. Rust port mirrors
# the signature; stage bodies are deferred (subprocess-heavy
# maintenance helpers not part of the runtime path).
create_release

# segments/common/players.rs::MPC_OUTPUT_RE — module-level
# OnceLock<Regex> mirroring the inline re pattern at
# powerline/segments/common/players.py:192-195
# `(.*) - (.*)\n\[([a-z]+)\] +[#0-9\/]+ +([0-9\:]+)\/([0-9\:]+)`.
# Python embeds the regex inline in MpdPlayerSegment.get_player_status
# at each call site; Rust extracts to a named constant so the
# OnceLock pattern doesn't recompile per call. Same shape already
# approved for CONFIG_FILE_NAME and TMUX_VAR_RE.
MPC_OUTPUT_RE

# segments/vim/mod.rs::SCHEME_RE — module-level OnceLock<Regex>
# mirroring the inline re.compile at
# powerline/segments/vim/__init__.py:219.
# Python exposes the compiled regex as a module attribute SCHEME_RE;
# Rust uses a fn returning &'static Regex so the same identifier is
# used at call sites. Same approved pattern as CONFIG_FILE_NAME,
# TMUX_VAR_RE, MPC_OUTPUT_RE.
SCHEME_RE

# mod.rs::LOG_KEYS mirrors the module-level set constant at
# powerline/__init__.py:402
# `LOG_KEYS = set(('log_format', 'log_level', 'log_file', 'paths'))`.
# Python exposes the set as a module attribute; Rust uses a fn
# returning &'static HashSet so the same identifier is preserved at
# call sites. Same approved pattern as CONFIG_FILE_NAME, TMUX_VAR_RE,
# MPC_OUTPUT_RE, SCHEME_RE.
LOG_KEYS

# mod.rs::Powerline::resolve_renderer_module — extracts the
# inline branch chain at powerline/__init__.py:486-495 that resolves
# the renderer_module argument to a fully-qualified module path.
# Python embeds the chain directly in the Powerline.init body;
# Rust factors it out to a pure helper so the 5 branches (None /
# undotted / leading-dot / trailing-dot / dotted) are independently
# testable without constructing a full Powerline instance.
resolve_renderer_module

# mod.rs::Powerline::load_colorscheme_config_levels — extracts the
# levels-tuple builder from the body of
# Powerline.load_colorscheme_config at powerline/__init__.py:806-810.
# Python embeds the tuple inline before passing to
# _load_hierarhical_config; Rust extracts to a pure helper so the
# 3-level pattern is independently testable without the full
# config_loader substrate (which is deferred).
load_colorscheme_config_levels

# mod.rs::Powerline::load_theme_config_levels — same extraction
# pattern for powerline/__init__.py:821-823 (the theme variant).
# Python builds the tuple inline; Rust extracts as a pure helper.
load_theme_config_levels

# segments/common/time.rs::unicode_text_translate — the Python source
# at powerline/segments/common/time.py:40-43 defines a static
# UNICODE_TEXT_TRANSLATION dict + calls str.translate(...). Rust has
# no equivalent str.translate; the helper applies the same two char
# substitutions inline. Used by fuzzy_time per py:96 / py:120.
unicode_text_translate

# segments/common/time.rs::fuzzy_time_default_hour_str /
# fuzzy_time_default_minute_str / fuzzy_time_default_special_cases —
# the Python source embeds the default values directly as keyword
# argument defaults in the fuzzy_time() signature at py:46-58. Rust
# has no Python-style mutable-default-arg pattern; defaults are
# extracted to standalone fns so they can be referenced at call sites
# without re-typing each one. Same name-prefixed-with-fn-name pattern
# used by other ports.
fuzzy_time_default_hour_str
fuzzy_time_default_minute_str
fuzzy_time_default_special_cases

# segments/common/time.rs::fuzzy_time_compute — extracts the
# value-computation core of fuzzy_time at py:93-122 (special-case
# lookup → rounding → closest-minute → format) so the algorithm is
# unit-testable without going through the libc::localtime_r I/O
# layer. fuzzy_time itself remains as the wrapper that reads
# localtime and dispatches to fuzzy_time_compute.
fuzzy_time_compute

# commands/config.rs::StrFunction::binding_function_name —
# Python's StrFunction at powerline/commands/config.py:9-18
# stores a callable via __init__(function, name) at py:10-12.
# The Rust enum can't carry a runtime callable across the
# (Source/Setenv/Setup/Command/Uses) variants directly, so the
# binding function name is exposed as a method that returns the
# corresponding `powerline.bindings.config.<name>` identifier.
# Used by callers (CLI driver, tests) to route the dispatch.
binding_function_name

# segments/common/env.rs::_geteuid — Python source at
# powerline/segments/common/env.py:163 defines _geteuid as a
# module-level binding via `getattr(os, 'geteuid', lambda: 1)`,
# resolving to either the real syscall or a fallback returning 1.
# Rust exposes it as a fn with the same identifier so the user()
# segment dispatcher can call it the same way Python does. The
# underscore-prefixed name preserves the module-private convention.
_geteuid

# renderers/shell/mod.rs::ShellRenderer::render_matcher_info —
# extracts the matcher_info pulling-from-segment-info logic from the
# body of ShellRenderer.render at
# powerline/renderers/shell/__init__.py:90-96. Python embeds the
# pull inline before delegating to super().render(); Rust extracts to
# a pure helper since the super().render() dispatch chain through the
# Renderer base is deferred. This lets callers test the matcher_info
# extraction independently. Same extraction-from-method-body pattern
# already approved for resolve_renderer_module.
render_matcher_info

# renderers/shell/mod.rs::ShellRenderer::do_render_resolve_style —
# extracts the used_term_escape_style resolution from
# ShellRenderer.do_render at
# powerline/renderers/shell/__init__.py:98-106. Python embeds the
# branch chain (term_escape_style == 'auto' + $TERM dispatch) inline
# before delegating to super().do_render(). Rust extracts the side
# effect (mutating self.used_term_escape_style) to its own method so
# it can be tested without the super dispatch substrate. Same
# pattern as load_colorscheme_config_levels.
do_render_resolve_style

# segments/common/net.rs::network_load_key — extracts the
# NetworkLoadSegment.key static method body at
# powerline/segments/common/net.py:198-200. Python's @staticmethod
# wrapper takes the **kwargs splat; the Rust port surfaces just the
# pure-functional `interface` → identity-string mapping. Renamed
# from plain `key` to avoid namespace collision with other
# `key` fns across the codebase.
network_load_key

# segments/common/net.rs::parse_proc_net_route_default — extracts
# the inline /proc/net/route scanner from
# NetworkLoadSegment.compute_state body at
# powerline/segments/common/net.py:208-216. Python embeds the
# file-open + line-scan inside compute_state; Rust extracts the
# scanner as a pure parser taking the file content so the
# default-route detection can be tested without filesystem I/O.
parse_proc_net_route_default

# segments/common/net.rs::pick_active_interface — extracts the
# activity-based interface picker from NetworkLoadSegment.compute_state
# body at powerline/segments/common/net.py:218-228. Python embeds
# the loop + rx+tx scoring + exclusion list (lo/vmnet/sit) inside
# compute_state; Rust extracts as a pure fn taking an iterator of
# (name, rx, tx) tuples so it can be tested without psutil/sysfs.
pick_active_interface

# segments/common/net.rs::sysfs_rx_path / sysfs_tx_path — extract the
# /sys/class/net/<iface>/statistics/{rx,tx}_bytes format strings from
# _get_bytes at powerline/segments/common/net.py:181/183. Python
# embeds the format inline; Rust pulls them to named helpers since
# they're useful at the call site and the format pattern is shared
# between both files.
sysfs_rx_path
sysfs_tx_path

# segments/common/net.rs::_get_bytes_sysfs — disambiguates the
# no-psutil branch of _get_bytes at
# powerline/segments/common/net.py:180-185. Python defines two
# _get_bytes fns under try/except ImportError (psutil branch at
# py:161-169, sysfs branch at py:180-185). Both share the Python
# fn name `_get_bytes`; Rust ports the sysfs branch as a pure
# parser taking rx/tx file contents, and the name is suffixed
# `_sysfs` to disambiguate from the psutil port that will land
# in a follow-on pass. Underscore prefix preserved.
_get_bytes_sysfs

# renderers/vim.rs::VimRenderer::get_matched_theme_cached —
# mutating variant of get_matched_theme that caches the constructed
# theme back into the local_themes match dict per
# powerline/renderers/vim.py:62 (`match['theme'] = Theme(...)`).
# Python's get_matched_theme is naturally mutating since dicts are
# passed by reference; Rust's borrow checker forces the cache-back
# step into a separate `&mut self` method because the read-only
# `get_matched_theme` returns the constructed value without
# touching the dict. Split mirrors the
# get_theme/get_theme_cached pattern used elsewhere.
get_matched_theme_cached
